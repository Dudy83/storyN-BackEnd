'use strict';

/**
 * category-suggestion service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::category-suggestion.category-suggestion');
